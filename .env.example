# Atlas-EDC Federation Proxy — Environment Configuration
# Copy this file to .env and adjust values for your deployment.

# --- Proxy ---
PORT=3000
LOCAL_WEBAPI_URL=http://localhost:8080

# --- EDC Connector Configuration (MVD) ---
# Consumer connector management API (from the host, via Kind ingress)
EDC_CONSUMER_MANAGEMENT_URL=http://127.0.0.1:8081/consumer/cp/api/management/v3

# Provider connector DSP protocol endpoint (K8s-internal address)
EDC_PROVIDER_PROTOCOL_URL=http://provider-qna-controlplane:8082/api/dsp

# Provider DID identity (used in catalog requests and contract negotiation)
EDC_PROVIDER_ID=did:web:provider-identityhub%3A7083:provider

# Management API key for the EDC connectors
EDC_API_KEY=password

# Asset ID registered on the provider connector
EDC_ASSET_ID=ohdsi-webapi-v3

# Override the data-plane endpoint when the proxy runs outside K8s.
# Leave empty to use the endpoint returned by the EDR token.
# Example: http://127.0.0.1:11002/api/public  (via kubectl port-forward)
EDC_PUBLIC_ENDPOINT_OVERRIDE=
